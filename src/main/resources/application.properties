spring.application.name=getgame
spring.config.import=optional:file:.env.properties
server.port =${APP_PORT}


spring.datasource.url=${DB_URL}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true
spring.profiles.active=${PROFILE}

#set up blob
azure.storage.connection.string=${AZURE_STORAGE_CONNECTION_STRING}
azure.storage.container.name=${AZURE_STORAGE_CONTAINER_NAME}
#azure.mysql.connection.string=${AZURE_MYSQL_CONNECTION_STRING}
#azure.mysql.username=${AZURE_MYSQL_USERNAME}
#azure.mysql.password=${AZURE_MYSQL_PASSWORD}